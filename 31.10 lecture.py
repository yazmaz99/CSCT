import turtle

userLength = int(input("Enter length of sides: "))

def drawSide(userLength):
    turtle.forward(userLength)
    turtle.right(90)
    return 

drawSide(userLength)
drawSide(userLength)
drawSide(userLength)
drawSide(userLength)

input()
